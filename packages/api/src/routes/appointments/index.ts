import { Hono } from 'hono'

// TODO: Add appointments routes
// Routes will follow: GET /, GET /:id, POST /, PUT /:id, DELETE /:id
// Also: POST /:id/confirm, POST /:id/cancel, POST /:id/complete

const router = new Hono()

export default router
