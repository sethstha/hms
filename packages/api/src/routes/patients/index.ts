import { Hono } from 'hono'

// TODO: Add patients routes
// Routes will follow: GET /, GET /:id, POST /, PUT /:id, DELETE /:id
// Also: GET /:id/visits, GET /:id/appointments, GET /:id/prescriptions

const router = new Hono()

export default router
